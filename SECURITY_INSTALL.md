# 荣耀手机安装说明

## 1.8.5 安全调整

- APK 使用“乡遇”专用正式证书签名，不再使用通用 Android 调试证书。
- 应用安装标识改为 `cn.xiangyu.travelguide`，避免继承旧安装标识可能存在的安全误报记录。
- 正式版只声明联网和粗略位置权限。
- 应用不读取或发送短信，不读取通讯录、电话、相册、文件、麦克风或摄像头，也不具备安装其他应用、悬浮窗或无障碍服务权限。
- 禁止明文网络传输、调试和应用数据备份。

## 从旧版迁移

1. 在旧版“收藏”页导出个人记事本。
2. 安装 1.8.5。新版可以和旧版同时存在。
3. 在新版“收藏”页导入刚才导出的记事本。
4. 确认资料完整后，再卸载旧版。

由于安装包未通过荣耀应用市场上架审核，侧载安装时仍可能显示“未经荣耀安全审核”的常规提醒。这与“应用已感染病毒”不是同一结论。

## 1.9.1 误报整改

- 再次核验正式 APK，清单中只有联网和粗略位置两项权限。
- 删除铁路查询和公开页面过滤逻辑中可能被静态扫描器误读的敏感文本，不改变应用功能范围。
- 禁止云备份、设备迁移、明文传输和调试，个人笔记仅保存在应用本机数据区。
- 分享功能只在用户点击“分享”后打开 Android 系统分享面板；应用自身不具备短信或通讯录读写能力。
- GitHub 自动构建新增权限白名单检查，若正式 APK 出现第三项权限会直接构建失败。

## 1.9.2 高德 POI

- 新增高德官方 POI 网络查询，但未新增任何 Android 权限；正式 APK 仍只有联网和粗略位置两项权限。
- Web 服务 Key 只从未纳入 Git 的 `local.properties` 注入，公开仓库和 GitHub 自动构建不保存 Key。
- 客户端直连服务时 Key 仍可能从 APK 或网络请求中被提取；面向大规模公开分发时应改用自有服务端代理，并在高德控制台设置配额和告警。
